void scheduler_tick() {
    // Tady bude logika pro přepínání mezi úlohami
    // Například můžete mít jednoduchý round-robin scheduler, který přepíná mezi dvěma úlohami.
    
    // Příklad pseudokódu:
    // 1. Uložení kontextu aktuální úlohy (registrové hodnoty, stack pointer, atd.)
    // 2. Výběr další úlohy k běhu (např. z fronty připravených úloh)
    // 3. Načtení kontextu nové úlohy a přepnutí na ni
    
    // Pro začátek můžete implementovat velmi jednoduchý scheduler, který pouze střídá dvě úlohy.
    return;
}